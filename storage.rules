rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Cho phép upload và tải ảnh báo cáo lỗi hiện trường
    match /reports/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
